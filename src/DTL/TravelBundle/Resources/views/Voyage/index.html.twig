{% extends "DTLTravelBundle::layout.html.twig" %}
{% block content %}
<h2>{{ resource.name }}</h2>
    {% for year in resource.years %}
    {{ render(controller('DTLTravelBundle:Voyage:timePeriodPreview', {'node': year})) }}
    {% endfor %}
{% endblock content %}

